# Feature

## 2026-04-14

- [x] 顶部导航栏默认的背景色需要和整个页面背景色做一个区分，让用户有感知
- [x] 切换星空色全局颜色样式时，内容显示不对，大部分内容都看不清了，需要做好适配
- [x] 首页的卡片内容，当没有图片时，箭头符号的样式需要优化，调小一点，现在太大一块了，有点难看

## Changelog

### 导航栏背景优化
- 为 Navbar 组件添加 `bg-[var(--card-bg)]` 背景色，与页面背景形成视觉区分

### 星空黑模式适配
- 调整 starry-dark 模式的颜色变量，提升整体亮度
- --page-bg: oklch(0.10...) (原 0.04)
- --card-bg: oklch(0.14...) (原 0.08)
- --text-color: oklch(0.92...) (原 0.90)
- --text-muted: oklch(0.65...) (原 0.60)
- 为 text-90/75/50/30/25 等常用文本颜色类添加 starry-dark 模式下的样式覆盖

### PostCard 箭头图标
- 将无封面文章卡片的进入按钮图标从 `text-4xl` 缩小到 `text-2xl`